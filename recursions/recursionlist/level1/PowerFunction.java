package recursions.recursionlist.level1;

public class PowerFunction {
    public static double power(int a, int n) {
        return Math.pow(a, n);
    }
    //Need to solve this with optimised from scaler
}
